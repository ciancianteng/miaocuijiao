---
description: 后台中心 Design Freeze — 禁止改 UI/布局，只允许业务与 Bug 修复
alwaysApply: true
---

# 后台中心 Design Freeze

自 2026-07-28 起，**【后台中心】** UI/布局已冻结。未经用户明确确认，禁止改视觉与布局。

## 冻结范围（禁止改）

- 整体布局、侧栏位置/宽度、Header、控制台首页布局
- 卡片大小/位置、页面间距、响应式断点
- 配色、字体大小、按钮/圆角/阴影/边框样式
- 后台 CSS、导航结构、整体视觉风格
- 删除现有后台页面、为新功能重新设计后台

涉及文件示例（触及即需遵守）：`admin.html`、`admin/**`、`src/admin-*.css`、`src/admin-*.js`、`src/admin-layout.css`、`src/admin-suite.*`、`src/admin-final-*`

## 允许继续（必须塞进现有布局）

- Bug / Supabase / 真实数据库接入
- 按钮功能、页面跳转
- 业务完善：老板/陪玩/客服/订单、Banner、公告、游戏分类、服务类型、优惠券、提现审核、系统设置、权限、统计
- 新增后台业务功能（沿用当前风格与 DOM 结构扩展，不重做壳子）

## 其他页面

- **老板端**：全面冻结（见 `boss-design-freeze.mdc`）。仅严重 Bug；订单详情弹窗不再优化。
- 客服端、陪玩端：P0 开发重点；设计和布局仍可改（除非另有冻结规则）。
- 后台中心：业务可继续，UI/布局仍冻结（本文件）。

## 执行原则

```
// ❌ 未经确认
改 admin-layout.css / 侧栏宽度 / 卡片样式 / 导航重组 / 重做后台壳

// ✅ 允许
只改 api/admin/*、数据绑定、事件逻辑；新模块复用现有 .admin-shell / 侧栏 / content 区域
```

若任务看起来需要改后台 CSS 或布局：先停下来，问用户确认，不要擅自改。
